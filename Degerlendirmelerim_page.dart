import 'package:flutter/material.dart';

class DegerlendirmelerimPage extends StatelessWidget {
  const DegerlendirmelerimPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Degerlendirmelerim'),
      ),
      body: const Center(
        child: Text('Degerlendirmelerim'),
      )
    );
  }
}